---
layout: post
title:  "Androidプログラミング 第5週"
tags: [24-1, Android_Studio]
---

## 授業内容

- ボタンの有効化および無効化
- ボタンの選択可否
- ボタンの回転
- テキストボックスの値を通知として出力する

<br>

- 筆者はすべての例を、チェックボックスとボタンを用いて実装した

<br>

### ボタンの有効化と無効化

- 有効化の有無は、**buttonName.setEnabled();** を使用して設定できる
  - setEnabled に **true** または **false** を指定することで、有効化の有無を決定できる
- 使用したコード

主な活動

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <CheckBox
 android:id="@+id/checkBox1"
 android:layout_width="wrap_content"
 android:layout_height="wrap_content"
 android:checked="true"
 android:text="有効化属性"
 android:textSize="30sp"/>
  </LinearLayout>  
~~~

- チェックボックスを作成する際、**android:checked**を使用すると、チェックボックスのチェック状態を設定できる
  - android:checkedには**true**または**false**を指定して、初期値を設定できる

~~~java
//MainActivity.java
package com.example.rotation_5nd_20214135;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.KeyEvent;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.CompoundButton;
import android.widget.EditText;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity {
    CheckBox cb1;
    Button btn1;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(R.layout.activity_main);

        cb1 = (CheckBox)findViewById(R.id.checkBox1);
 btn1 = (Button) findViewById(R.id.btn1);

        //チェック状態の変更時に処理を実行
 cb1.setOnCheckedChangeListener(new CompoundButton.OnCheckedChangeListener() {
 @Override
 public void onCheckedChanged(CompoundButton buttonView, boolean isChecked) {
 //チェック状態を確認
                if(isChecked)
 btn1.setEnabled(true); //有効化処理
 else
 btn1.setEnabled(false); //無効化処理
 }
 });
    } //onCreate
} //MainActivity
~~~

<br>

**ボタンの有効化**

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/ButtonEnable.png )

- チェックボックスを選択してオブジェクトを有効にすると、色が紫（デフォルト）に変わり、選択可能になる

**ボタンを無効にする**

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/ButtonDisable.png )

- チェックボックスを選択してオブジェクトが無効化されると、灰色に変わり、選択できなくなります



<br/>

### オブジェクトの選択可否

- サンプルプログラムでは、チェックボックスの選択状態に応じて、ボタンの選択可能状態が切り替わるように実装しました

- オブジェクトのクリック可否は、**オブジェクト名.setClickable();** を使用して設定できます
  - setClickable に **true** または **false** を指定することで、クリック可能かどうかを決定できます

- オブジェクトの選択を無効に設定した場合、無効化されたときとは異なり、色は変わらないが選択できなくなる

<br>

**使用したコード**

主な活動

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <CheckBox
 android:id="@+id/checkBox2"
 android:layout_width="wrap_content"
 android:layout_height="wrap_content"
 android:checked="true"
 android:text="クリック可能属性"
 android:textSize="30sp"/>
  </LinearLayout>  
~~~



~~~java
//MainActivity.java
package com.example.rotation_5nd_20214135;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.KeyEvent;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.CompoundButton;
import android.widget.EditText;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity {
    CheckBox cb2;
    Button btn1;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(R.layout.activity_main);

        cb2 = (CheckBox)findViewById(R.id.checkBox2);
 btn1 = (Button) findViewById(R.id.btn1);

        //チェック状態が変更された際に実行する処理
 cb2.setOnCheckedChangeListener(new CompoundButton.OnCheckedChangeListener() {
 @Override
 public void onCheckedChanged(CompoundButton buttonView, boolean isChecked) {
 if(isChecked)
                    btn1.setClickable(true); //有効化処理
 else
 btn1.setClickable(false); //無効化処理
 }
 });
    } //onCreate
} //MainActivity
~~~

**サンプルプログラムでの動作例**

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/ButtonSelectable.png )

<br>

### オブジェクトの回転

- オブジェクトの回転は、**オブジェクト名.setRotation(角度);** を使用して実装できる
- サンプルプログラムでは、チェックボックスの選択状態に応じて、45度と0度に設定するように実装した

**使用したコード**

~~~xml
<!-- activity_main.xml -->
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <Button
 android:id="@+id/btn1"
 android:layout_width="wrap_content"
 android:layout_height="wrap_content"
 android:text="Button"
 android:textSize="30sp"
 android:layout_marginTop="100dp"/>
</LinearLayout>
~~~

~~~java
//MainActivity.java
package com.example.rotation_5nd_20214135;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.KeyEvent;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.CompoundButton;
import android.widget.EditText;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity {
    CheckBox cb3;
    Button btn1;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(R.layout.activity_main);

        cb3 = (CheckBox)findViewById(R.id.checkBox3);
 btn1 = (Button) findViewById(R.id.btn1);

        cb3.setOnCheckedChangeListener(new CompoundButton.OnCheckedChangeListener() {
 @Override
 public void onCheckedChanged(CompoundButton buttonView, boolean isChecked) {
 if(isChecked)
                    btn1.setRotation(45); //45度回転
 else
 btn1.setRotation(0); //無効化処理
 }
 });
    } //onCreate
} //MainActivity
~~~

**サンプルプログラムでの動作例**

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/ButtonEnable.png )

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/ButtonRotate.png )

<br>

### テキストボックスの値を通報として出力する

- サンプルプログラムでは、キーボードから手を離すと、入力したメッセージが通知として表示される
- サンプルプログラムでは**ACTION_UP**を使用しているが、ACTION_DOWNなども使用可能である

**使用したコード**

~~~xml
<!-- activity_main.xml -->
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <EditText
 android:id="@+id/edtText1"
 android:layout_width="match_parent"
 android:layout_height="wrap_content"
 android:textSize="30sp"/>
</LinearLayout>
~~~

~~~java
//MainActivity.java
package com.example.rotation_5nd_20214135;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.KeyEvent;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.CompoundButton;
import android.widget.EditText;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity {
    EditText edtText1;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
 super.onCreate(savedInstanceState);
 setContentView(R.layout.activity_main);
 
 edtText1 = (EditText) findViewById(R.id.edtText1);
        edtText1.setOnKeyListener(new View.OnKeyListener() {
 @Override
 public boolean onKey(View v, int keyCode, KeyEvent event) {
 if(event.getAction() == KeyEvent.ACTION_UP)
                    Toast.makeText(getApplicationContext(), edtText1.getText().toString(), Toast.LENGTH_SHORT).show();

                return false;
 }
 });
    } //onCreate
} //MainActivity
~~~

**サンプルプログラムでの動作例**

![画像の代替テキスト]({{link}}/assets/img/Android/5nd/TextEvent.png )
