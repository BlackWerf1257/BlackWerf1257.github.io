---
layout: post
title:  "C++ Week 2: Functions"
tags: [23-2, Cpp]
---

## Functions

### Definition

A block of code that performs a specific action <br/>

### How to Use

``` c++
반환타입 함수이름(함수파라미터);
```

Ex) void TestVoid(){ };

<br/>

```c++
#include <iostream>
int main(){
 std::cout <<”Hello C++”;
 return 0;
}
```

- You can omit the `return` statement in the `main` function



## Preprocessor

- A process in which a program transforms code according to certain rules before compilation

  ​  Ex) include => #include <iostream>

  #### Types

  - \#include
  - #define
  - #inline
  - #if
  - #ifdef
  - #ifndef



## Namespace

- One solution for resolving name conflicts

- Added to the C++ standard in 2003

- Developers can create and distinguish their own spaces

  <br/>

standard

- The standard namespace in C++
- Declared within the iostream header file
- Related functions include **cout, cin, and end**
- Can be omitted when declaring std using the using directive



## Basic Information About Computers

- Bit: The smallest unit of information processing in a computer
- Optimized bit: The unit that a CPU can process at a time

<br/>

## Variables 

### Definition

- Space required to store data

- No location restrictions; a prominent placement for the function is recommended

### How to Declare

- variable_type variable_name;
- Example)

```c++
int a;
char b;
```

<br/>

### Naming Conventions

- Use names that clearly convey their purpose
- Avoid names that can be interpreted in multiple ways
  - e.g., accountList
- Do not use hyphens
- Start with a lowercase letter
- Use nouns (or noun phrases) for class names
- Use verbs (or verb phrases) for method names, and start with a verb

<br/>

### Types of Variables

|  구분  |     자료형     | 크기  | 범위 |
| :----: | :------------: | :---: | :------------------------------------------------: |
|   -    | void |   -   | - |
| 문자형 | char | 1byte | -128 ~ 127 |
| | unsigned char  | | 0 ~ 255 |
| 정수형 | bool | | 0~1 |
| |     short | 2byte | -32,768 ~ 32,767 |
| | unsigned short | 4byte | 0 ~ 65,535 |
| | int | | -2,147,483,648 ~ 2,147,483,647 |
| |  unsigned int  | | 0 ~ 4,294,967,295 |
| | long | | -2,147,483,648 ~ 2,147,483,647 |
| | unsigned long  | | 0 ~ 4,294,967,295 |
| |     float | |     3.4E-38(-3.4*10^38) ~ 3.4E+38(3.4 * 10^38)     |
| |     double     | 8byte | 1.79E-308(-1.79*10^308) ~ 1.79E+308(1.79 * 10^308) |

<br/>

## Strings

- Array (String X):  

  - Example) 
 - char data1[10] = { 'a', 'b', 'c', 'd', 'e'}; 

- CString

  - Example)

    - char data2[10] = {'a','b','c','d','e', '\0'};

    - char data3[10] = "abcde";

      <br/>

      | **data1** | **a** | **b** | **c** | **d** | **e** | **!**  | **@**  | **^**  | **#**  | **$**  |
 | --------- | ----- | ----- | ----- | ----- | ----- | ------ | ------ | ------ | ------ | ------ |
 | **data2** | **a** | **b** | **c** | **d** | **e** | **\0** | **@**  | **^**  | **#**  | **$**  |
      | **data3** | **a** | **b** | **c** | **d** | **e** | **\0** | **\0** | **\0** | **\0** | **\0** |

### Commands

- Example)

  ```c++
  #include <string.h>
  int n = strlen("test")
  ```

  - **strcmp()**
    - A function that compares strings
    - Syntax: **strcmp(string1, string2)**
    - Result:
 - If the value is greater than 0: string1 is greater than string2
 - If the value is 0: string1 is equal to string2
      - If the value is less than 0: string1 is less than string2
  - **strlen()**
    - A function that returns the length of a string
    - Syntax: **strlen(string value)**
    - Result: Returns the length of the string value passed as a parameter
  - **strcpy()**
    - A function that copies a string
    - Usage: **strcpy(string1, string2)**
    - Result: Copies the value of string2 to string1
 
 <br/>

### Input and Output

#### Input

to eat

#### Output

- cost

  <br/>

#### Sample Code

```c++
#include <iostream>
using namespace std;

int main(){
    cout << "글자를 입력하세요>>";
    char name[11]; //한글 - 5글자 | 영문 - 10문자
    cin >> name;
    
    cout << "출력된 문자 >> " << name << endl;
}
```

**Results**

~~~
Enter text >> Arnold 
Displayed text >> Arnold
~~~

<br/>

#### Enter a string that includes spaces

-  **cin.getline(char buf[], int size, char delimitChar)**

  - Enter a character one size smaller than `buf`

  - Input is terminated when a delimitChar is encountered
    - Both delimitChar and buf are terminated with '\0'
  - The default value for delimitChar is **'\n' (Enter)**

  <br/>

  - #### Sample Code

    ```c++
    #include <iostream>
    using namespace std;
    
    int main(){
        cout << "글자를 입력하세요>>";
        char name[20];
    	cin.getline(name, 100, '\n');
        
        cout << "출력된 문자 >> " << name << endl;
    }
    ```

    **Results**

    ~~~
    Enter text >> Arnold Michael 
 Output >> Arnold Michael 
 ~~~

    <br/>

<br/>

### The String Class

- Unlike `char`, there is no limit on string length
- Provides various functions and operators for string copying, comparison, and more
- Is object-oriented
- Requires including the `string.h` header



#### Sample Code

```c++
#include <iostream>
#include <string>
using namespace std;

int main(){
    string University("백석대학교");
    string Department("첨단IT 학부");
    string Major("AR/VR");
    
    cout << "현재 " << University << " " << Department << " " << Major << "전공으로 재학중입니다 " << endl;
} 
```

**Results**

```
현재 백석대학교 첨단IT 학부 AR/VR전공으로 재학중입니다
```

